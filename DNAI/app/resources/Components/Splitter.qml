import QtQuick 2.0

import DNAI 1.0

Rectangle {
    border.width: 2
    border.color: AppSettings.theme["text"]["accentColor"]
    anchors.left: parent.left
    anchors.right: parent.right
    height: 2
}
